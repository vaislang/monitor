{
  "name": "@vais-apps/monitor-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test --config=e2e/playwright.config.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vaisx/plugin": "file:../../../vais/lang/packages/vais-web/packages/plugin",
    "typescript": "~5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
